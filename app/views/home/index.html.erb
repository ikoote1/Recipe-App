<!DOCTYPE html>
<html>
<head>
  <title>Welcome to Recipe</title>
</head>
<body>
  <%= link_to "Sign Up", new_user_registration_path %>
  
  <% if user_signed_in? %>
    <%= form_tag(destroy_user_session_path, method: :delete) do %>
      <%= submit_tag "Sign Out" %>
    <% end %>
  <% else %>
    <%= link_to "Login", new_user_session_path %>
  <% end %>
</body>
</html>
